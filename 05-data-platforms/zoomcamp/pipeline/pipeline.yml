name: nyc-taxi
schedule: "daily"
start_date: "2021-01-01"

default_connections:
  bigquery: gcp-default
  duckdb: duckdb-default

variables:
  taxi_types:
    type: array
    items:
      type: string
    default: ["yellow"]